const calcularVelocidad = (distancia, tiempo) => {
//   const distancia = 10;
//   const tiempo = 2;
    const velocidad = distancia / tiempo;
    console.log("La velocidad es " + velocidad);
    return velocidad;
}

velocidad1 = calcularVelocidad(10,2);
velocidad2 = calcularVelocidad(100,2);
velocidad3 = calcularVelocidad(75,3);
